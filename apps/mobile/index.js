/**
 * @format
 */

import { AppRegistry } from 'react-native';
import MobileRoot from './MobileRoot';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => MobileRoot);
